import React, { Component } from 'react'
import NewForm from '../components/NewPageForm'

export default class Footer extends Component {
    render() {
        return (
            <footer className="footer">
                <NewForm />
            </footer>
        )
    }
}